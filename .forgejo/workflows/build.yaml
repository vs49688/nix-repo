on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  check:
    runs-on: native
    steps:
      - name: Clone Repository
        uses: actions/checkout@v4
      - name: Build
        run: nix flake check
