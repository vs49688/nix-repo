on:
  push:
  pull_request:
  workflow_dispatch:
jobs:
  check:
    runs-on: nixos
    steps:
      - name: Clone Repository
        uses: actions/checkout@v4
      - name: Build
        run: nix flake check
